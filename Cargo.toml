[package]
name = "polyq_backend"
version = "0.1.0"
edition = "2021"

[lib]
name = "polyq_backend"
# Default library crate type (rlib) — Python interfacing removed

[dependencies]
num-complex = "0.4"
rand = "0.8"


[[bin]]
name = "benchmarker"
path = "Rust_Benchmark/Benchmakr.rs"
