numpy>=2
qiskit>=2.0.0
pytest

# PolyQ on itself only requires numpy for evaluation and qiskit for it's QuantumCicuit object.
# For benchmarking and testing purposes, one will need the following libraries as intended.

qiskit_aer>=0.17.0
mqt>=0.1.1
mqt.ddsim>=1.24
psutil>=7.0.0
